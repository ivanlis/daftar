<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
          package="bilbao.ivanlis.daftar">

    <!--<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>-->

    <application
            android:name="bilbao.ivanlis.daftar.KobetaApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_kobeta"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_kobeta_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Daftar">
        <activity android:name="bilbao.ivanlis.daftar.MainActivity"
                  android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>